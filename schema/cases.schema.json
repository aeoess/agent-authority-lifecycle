{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/aeoess/agent-authority-lifecycle/schema/cases.schema.json",
  "title": "Agent Authority Lifecycle case catalog",
  "description": "The single source of truth for the case catalog. CASES.md and BOUNDARY-CASES.md are generated from this file by scripts/build_cases_md.py and must not be edited by hand. One object per case, grouped by semantic_family (the authority question the case asks) with the pre-v0.3 grouping kept as fixture_family. No object in this file is itself a conformance result; `fixtures` links out to the Agent Authority Conformance suite. A fixture entry records a lab decision (VECTOR, COVERED or RESEARCH_ONLY), the fixture family, the vector ids and a summary of what the two reference SDKs decided. Entries with `pending_pr` true have no published branch yet, so no link is given.",
  "type": "array",
  "items": {
    "$ref": "#/$defs/case"
  },
  "$defs": {
    "caseId": {
      "type": "string",
      "pattern": "^LC-[A-Z]-[0-9]{3}$"
    },
    "invariantId": {
      "type": "string",
      "pattern": "^L([1-9]|1[0-2])$"
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "url",
        "quote",
        "fetched_by"
      ],
      "properties": {
        "url": {
          "type": "string",
          "format": "uri"
        },
        "quote": {
          "type": "string",
          "minLength": 1,
          "description": "Verbatim quote, 40 words or fewer, from the fetched source. Word count is checked by scripts/validate_cases.py, not expressible in JSON Schema alone."
        },
        "fetched_by": {
          "type": "string",
          "enum": [
            "auditor",
            "thin-group ledger"
          ]
        }
      }
    },
    "variant": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "difference"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/caseId"
        },
        "difference": {
          "type": "string",
          "minLength": 1,
          "description": "The one-line difference that made this id a duplicate of the representative case rather than a separate entry."
        }
      }
    },
    "related": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "invariants",
        "open_questions",
        "concepts",
        "text"
      ],
      "properties": {
        "invariants": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/invariantId"
          },
          "uniqueItems": true
        },
        "open_questions": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "teardown-completeness",
              "work-in-flight",
              "release-from-suspension",
              "critical-revocation",
              "office-vacancy-and-succession",
              "grants-signed-just-before-departure",
              "authority-rollback",
              "semantic-drift",
              "notice-and-relying-parties"
            ]
          },
          "uniqueItems": true
        },
        "concepts": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "uniqueItems": true,
          "description": "Slugs of AUTHORITY-LIFECYCLE.md glossary terms (the 'Lifecycle concepts are separate' section) mentioned in this case's related-invariant or boundary-reason text."
        },
        "text": {
          "type": "string",
          "description": "Verbatim text of the case's 'Related invariant/open question' or boundary-reason paragraph. Source of the parsed invariants/open_questions/concepts above."
        }
      }
    },
    "case": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "title",
        "semantic_family",
        "domain",
        "fixture_family",
        "tier",
        "status",
        "situation",
        "source_prose_label",
        "source_prose",
        "expected_outcome",
        "naive_failure",
        "related",
        "sources",
        "variants",
        "fixtures"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/caseId"
        },
        "title": {
          "type": "string",
          "minLength": 1
        },
        "semantic_family": {
          "type": "string",
          "enum": [
            "creation-and-activation",
            "issuer-standing-and-authority-to-change",
            "dependencies-and-subdelegation",
            "succession-and-replacement",
            "collective-and-multi-principal-authority",
            "suspension-restriction-and-release",
            "revocation-expiry-and-exhaustion",
            "external-authority-changing-events",
            "notice-observation-and-reliance",
            "credentials-keys-and-derived-authority",
            "in-flight-actions-and-authorization-boundaries",
            "scheduled-and-dormant-authority",
            "replication-rollback-and-stale-state",
            "identity-target-and-capability-drift",
            "policy-version-and-rule-change",
            "retroactive-findings-and-recharacterization",
            "time-clocks-and-validity-windows",
            "evidence-attribution-and-completeness"
          ],
          "description": "The authority question this case asks, derived from the cases themselves in the v0.3 pass. Exactly one per case, including boundary cases, where it records the nearest authority question rather than a claim that the case resolves to an authority verdict. Defined in drafts/TAXONOMY.md and used to group CASES.md."
        },
        "domain": {
          "type": "string",
          "enum": [
            "agency-law",
            "estates-and-trusts",
            "corporate",
            "banking-payments",
            "insolvency",
            "civil-procedure",
            "regulatory",
            "government",
            "military",
            "aviation",
            "medicine",
            "maritime",
            "industrial-safety",
            "security-incident",
            "distributed-systems",
            "cryptography-and-pki",
            "platform-engineering",
            "naming-and-registry",
            "agent-native"
          ],
          "description": "The source domain the case is drawn from. Metadata only: it records where the evidence comes from, not what the case decides. For an unsourced hypothetical it records the domain the scenario is set in."
        },
        "fixture_family": {
          "type": "string",
          "minLength": 1,
          "description": "The pre-v0.3 grouping heading this case sat under in CASES.md or BOUNDARY-CASES.md, kept so lab fixture family directory names stay resolvable. Superseded by semantic_family for grouping."
        },
        "tier": {
          "type": "string",
          "enum": [
            "verified",
            "hypothetical",
            "candidate",
            "boundary"
          ],
          "description": "verified, hypothetical and candidate mark a case that lives in CASES.md. Boundary means the case lives in BOUNDARY-CASES.md (it resolves to a liability, detection-surface or evidence question, not an authority verdict). verified requires at least one source with a quote. hypothetical requires zero sources and an explicit no-external-precedent statement in notes. candidate requires a 'Known issue' marker in notes recording the claimed-but-unverified source state."
        },
        "status": {
          "type": "string",
          "enum": [
            "proposed"
          ],
          "description": "Fixture/build-maturity status. Proposed unless a runnable fixture exists in the Agent Authority Conformance suite; none does yet for any case, so this is 'proposed' throughout. Distinct from `tier`, which is about sourcing confidence."
        },
        "out_of_scope_reason": {
          "type": "string",
          "enum": [
            "security-control",
            "liability-or-legal-consequence",
            "execution-and-scheduler-mechanics",
            "no-verifier-in-the-scenario",
            "implementation-or-configuration-defect",
            "no-authority-transition-in-the-scenario"
          ],
          "description": "Why this case is a boundary case and not a lifecycle case. Required when tier is boundary, forbidden otherwise. BOUNDARY-CASES.md is grouped by this field."
        },
        "situation": {
          "type": "string",
          "description": "What triggers the case. In the short reason-plus-source form this paragraph is both the situation and the boundary reason, and related.text repeats it verbatim. The generator renders it once, under a **Reason.** label."
        },
        "source_prose_label": {
          "type": "string",
          "enum": [
            "Human analog.",
            "Source.",
            "Source verified.",
            ""
          ],
          "description": "The label the case's source paragraph carries in the generated Markdown. Empty string only when source_prose is empty."
        },
        "source_prose": {
          "type": "string",
          "description": "The case's source paragraph as Markdown: the named statute, standard, opinion or incident report, the link, and the quoted sentence in context. Every url in `sources` appears in this text. Empty only for a case that cites nothing."
        },
        "expected_outcome": {
          "type": "string",
          "description": "What a verifier should return. Empty for BOUNDARY-CASES.md's short-form entries, which do not state one (that is what makes them boundary, not lifecycle, cases)."
        },
        "naive_failure": {
          "type": "string",
          "description": "What a naive system gets wrong. Empty for BOUNDARY-CASES.md's short-form entries."
        },
        "related": {
          "$ref": "#/$defs/related"
        },
        "sources": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/source"
          },
          "description": "Empty only when tier is hypothetical (an honestly-labelled hypothetical with the legal or incident claim removed, survival criterion 3's second branch), never for a case that states a sourceable claim."
        },
        "variants": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/variant"
          }
        },
        "fixtures": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/fixture"
          },
          "description": "One entry per fixture family the lab wave mapped this case onto. Empty for BOUNDARY-CASES.md entries, which the lab wave did not cover."
        },
        "notes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional. Carries the source document's own literal status line and any 'Known issue' / inline annotation text verbatim, so nothing found during parsing is silently dropped."
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "tier": {
                "const": "boundary"
              }
            },
            "required": [
              "tier"
            ]
          },
          "then": {
            "required": [
              "out_of_scope_reason"
            ]
          },
          "else": {
            "not": {
              "required": [
                "out_of_scope_reason"
              ]
            }
          }
        }
      ]
    },
    "fixture": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "decision",
        "family",
        "vector_ids",
        "sdk_results",
        "pending_pr"
      ],
      "properties": {
        "decision": {
          "type": "string",
          "enum": [
            "VECTOR",
            "COVERED",
            "RESEARCH_ONLY"
          ],
          "description": "The lab wave's decision for this case against this fixture family. VECTOR means new vectors were built for it, COVERED means an existing family already tests the same question, RESEARCH_ONLY means no record set decides it."
        },
        "family": {
          "type": [
            "string",
            "null"
          ],
          "description": "Fixture family directory name in the Agent Authority Conformance suite. Null only for a RESEARCH_ONLY decision, where no family exists."
        },
        "vector_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "uniqueItems": true,
          "description": "Vector ids in that family. Empty where the coverage table names the family without naming vectors."
        },
        "sdk_results": {
          "type": "string",
          "description": "Summary of what the npm and PyPI reference SDKs decided for this case, copied from the lab coverage table. Not a conformance claim."
        },
        "pending_pr": {
          "type": "boolean",
          "description": "True where the fixture sits on an unmerged candidate branch with no settled branch name to link, so no url is given. False where the fixture is already on the suite's main branch and `url` carries a pinned link."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "Pinned link to the fixture README on the suite's main branch. Present only when pending_pr is false."
        },
        "note": {
          "type": "string",
          "description": "Optional qualifier from the lab coverage table."
        }
      }
    }
  }
}
